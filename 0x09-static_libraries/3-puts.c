#include "main.h"

/**
 * _puts - takes in an array of pointers to chars and
 * prints them to stdout using _putchar
 * @str: pointer to the string
 *
 * Return: Nothing
 */

void _puts(char *str)
{
	while (*str != '\0')
	{
		str++;
	}
}
