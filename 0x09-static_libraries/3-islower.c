#include "main.h"

/**
 * _islower - checks if an integer is lower than another?
 * @c: number being checked out
 *
 * Return: integer. 0 if false, 1 if true
 */

int _islower(int c)
{
	/*
	 * do some really cool stuff here
	 */
	if (c < 0)
		return (1);
	return (0);
}
