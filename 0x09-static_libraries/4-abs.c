#include "main.h"

/**
 * _abs - returns the absolute value of a number
 * @n: number being modified and checked
 *
 * Return: the absolute value of @n
 */

int _abs(int n)
{
	/*
	 * do some awesome stuff here again pls
	 */
	n = (n >= 0) ? n : -1 * n;
	return (n);
}
